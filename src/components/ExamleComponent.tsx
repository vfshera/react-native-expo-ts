import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ExamleComponent = () => {
  return (
    <View>
      <Text>ExamleComponent</Text>
    </View>
  );
};

export default ExamleComponent;

const styles = StyleSheet.create({});
